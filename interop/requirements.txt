# Cross-check toolchain ONLY (never authors committed FITS bytes — see README determinism note).
# Pinned below the major versions whose byte/output behaviour the goldens were checked against.
# This is NOT a Zig build dependency; install into a throwaway venv:
#   python3 -m venv /tmp/astropy-venv && /tmp/astropy-venv/bin/pip install -r requirements.txt
astropy<9
numpy<3
